name: GitHub Actions Demo
run-name: ${{ github.actor }} กำลังทดสอบ GitHub Actions 🚀 
on: [push]

jobs:
  Explore-GitHub-Actions: 
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 งานถูกเรียกใช้งานโดยอัตโนมัติจากเหตุการณ์ ${{ github.event_name }}"
      - run: echo "🐧 งานนี้กำลังทำงานบนเซิร์ฟเวอร์ ${{ runner.os }} ที่โฮสต์โดย GitHub!"
      - run: echo "🔎 ชื่อสาขาของคุณคือ ${{ github.ref }} และที่เก็บข้อมูลของคุณคือ ${{ github.repository }}"
      - name: ตรวจสอบรหัสที่เก็บข้อมูล
        uses: actions/checkout@v4
      - run: echo "💡 รีพอริทอรี ${{ github.repository }} ได้ถูกโคลนไปยังรันเนอร์แล้ว"
      - run: echo "🖥️ เวิร์กโฟลว์พร้อมที่จะทดสอบโค้ดของคุณบนตัวรันเนอร์แล้ว"
      - name: รายชื่อไฟล์ในที่เก็บ
        run: |
          ls ${{ github.workspace }} 

  deploy:
    needs: Explore-GitHub-Actions
    runs-on: self-hosted   # เปลี่ยนเป็น self-hosted runner ของคุณ
    environment:
      name: production
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: 'zulu'
      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~\.sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar
      - name: Cache SonarQube scanner
        id: cache-sonar-scanner
        uses: actions/cache@v4
        with:
          path: .\.sonar\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner
      - name: Install SonarQube scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path .\.sonar\scanner -ItemType Directory
          dotnet tool update dotnet-sonarscanner --tool-path .\.sonar\scanner

      - name: Build and analyze
        shell: powershell
        run: |
          # หยุด workflow เมื่อเจอ error
          $ErrorActionPreference = "Stop"
          $env:SONAR_TOKEN="${{ secrets.SONAR_TOKEN }}"

          # Begin SonarScanner
          .\.sonar\scanner\dotnet-sonarscanner begin `
            /k:"github-action" `
            /d:sonar.login=$env:SONAR_TOKEN `
            /d:sonar.host.url=http://localhost:9000

          # Build project (fail on warning/error)
          dotnet build /warnaserror

          # End SonarScanner
          .\.sonar\scanner\dotnet-sonarscanner end /d:sonar.login=$env:SONAR_TOKEN


          $projectKey = "github-action"
          $encodedProjectKey = $encodedProjectKey = [uri]::EscapeDataString("${{ secrets.SONAR_TOKEN }}")

          $headers = @{
            Authorization = "Basic " + [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("${{ secrets.SONAR_TOKEN }}"))
          }

          $response = Invoke-RestMethod -Uri "http://localhost:9000/api/qualitygates/project_status?projectKey=$encodedProjectKey" -Headers $headers

          $status = $response.projectStatus.status

          if ($status -ne "OK") {
            Write-Error "❌ Quality Gate failed with status $status"
          exit 1
          } else {
            Write-Output "✅ Quality Gate passed"
          }
